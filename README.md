# Welcome to sample_ml_project 👋

Sample ML Project with Backend, Frontend, and a Database.

## Usage

Make sure docker is installed by running `docker info`, then run the project simply by:

```bash
docker compose up --build
```
